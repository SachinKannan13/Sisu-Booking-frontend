OMRTechPark from booksphere-client. Use via `window.Booksphere.OMRTechPark` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BooksphereProvider>` (full provider chain in README.md — components read theme/i18n from that context).
