import { BookUpload } from 'booksphere-client';

export const Default = () => (
  <div style={{ padding: '24px', background: '#1a1a1a', maxWidth: '480px', borderRadius: '12px' }}>
    <BookUpload />
  </div>
);
