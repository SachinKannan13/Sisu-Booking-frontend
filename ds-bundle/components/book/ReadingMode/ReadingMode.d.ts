import * as React from 'react';

/**
 * ReadingMode — from booksphere-client@1.0.0.
 */
export interface ReadingModeProps {
  [key: string]: unknown;
}

export declare const ReadingMode: React.ComponentType<ReadingModeProps>;
