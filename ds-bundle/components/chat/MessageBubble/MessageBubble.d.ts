import * as React from 'react';

/**
 * MessageBubble — from booksphere-client@1.0.0.
 */
export interface MessageBubbleProps {
  [key: string]: unknown;
}

export declare const MessageBubble: React.ComponentType<MessageBubbleProps>;
