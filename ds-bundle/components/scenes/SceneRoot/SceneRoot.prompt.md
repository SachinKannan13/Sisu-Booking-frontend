SceneRoot from booksphere-client. Use via `window.Booksphere.SceneRoot` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BooksphereProvider>` (full provider chain in README.md — components read theme/i18n from that context).

Generic animated wrapper every scene root uses for ambient drift + fade-in.
