SkyBackground from booksphere-client. Use via `window.Booksphere.SkyBackground` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<BooksphereProvider>` (full provider chain in README.md — components read theme/i18n from that context).

Standard sky background rect + gradient defs id, shared by every scene.
